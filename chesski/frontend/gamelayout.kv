# RelativeLayout
<GameLayout>:
    size_hint: None, None
    # centering in parent layout
    size: (min(self.parent.height, self.parent.width), min(self.parent.height, self.parent.width))
    pos_hint: {'center_x': .5, 'center_y': .5}
